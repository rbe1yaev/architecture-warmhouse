@startuml
!includeurl https://raw.githubusercontent.com/plantuml-stdlib/C4-PlantUML/master/C4_Context.puml

title C4 Context Diagram (AS-IS) — система "Тёплый дом"

LAYOUT_LEFT_RIGHT()

Person(user, "Пользователь", "Управляет отоплением дома и просматривает температуру через приложение")
Person(installer, "Специалист по подключению", "Выезжает в дом и вручную подключает оборудование к системе")

System(warm_home, "Тёплый дом", "Монолитное приложение на Go", "Текущая версия платформы для удалённого управления отоплением дома и запроса температуры")
System_Ext(devices, "Датчики и контроллеры отопления", "Физические устройства в доме", "Принимают команды от сервера и отдают данные о температуре только по запросу")

Rel(user, warm_home, "Управляет отоплением, запрашивает температуру", "Web / Mobile App")
Rel(installer, warm_home, "Подключает дом и настраивает оборудование", "Служебный интерфейс / ручные операции")
Rel(warm_home, devices, "Отправляет команды, запрашивает температуру", "Синхронные запросы")
Rel(devices, warm_home, "Возвращают температуру и статус", "Ответ на запрос")

SHOW_LEGEND()
@enduml